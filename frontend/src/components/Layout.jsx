import { NavLink, Outlet, useNavigate } from 'react-router-dom';
import { LayoutDashboard, ShoppingCart, Users, Warehouse, UserRoundCog, Clock3, FileText, Settings, Moon, Sun, LogOut, Menu, X, Shirt } from 'lucide-react';
import { useState } from 'react';
import { useStore } from '../store';
const links = [['/', 'ড্যাশবোর্ড', LayoutDashboard], ['/orders','অর্ডার',ShoppingCart], ['/customers','কাস্টমার',Users], ['/inventory','ইনভেন্টরি',Warehouse], ['/employees','কর্মী',UserRoundCog], ['/attendance','হাজিরা',Clock3], ['/reports','রিপোর্টস',FileText], ['/settings','সেটিংস',Settings]];
export default function Layout() { const [open,setOpen]=useState(false); const theme=useStore(s=>s.theme); const setTheme=useStore(s=>s.setTheme); const logout=useStore(s=>s.logout); const navigate=useNavigate(); return <div className="app-shell"><aside className={open?'sidebar open':'sidebar'}><div className="brand"><div className="brand-mark"><Shirt size={22}/></div><div><strong>Two M-s</strong><small>VEIL FACTORY</small></div><button className="icon-btn close-mobile" onClick={()=>setOpen(false)}><X size={19}/></button></div><nav>{links.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/'} onClick={()=>setOpen(false)}><Icon size={18}/><span>{label}</span></NavLink>)}</nav><div className="sidebar-bottom"><button className="theme-toggle" onClick={()=>setTheme(theme==='dark'?'light':'dark')}>{theme==='dark'?<Sun size={18}/>:<Moon size={18}/>}<span>{theme==='dark'?'লাইট মোড':'ডার্ক মোড'}</span></button><button className="logout" onClick={()=>{logout();navigate('/login')}}><LogOut size={18}/><span>লগ আউট</span></button></div></aside><main className="main"><header className="mobile-header"><button className="icon-btn" onClick={()=>setOpen(true)}><Menu size={20}/></button><span>Two M-s Veil</span></header><Outlet/></main></div> }
