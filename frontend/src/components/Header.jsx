import { Bell, ChevronDown, Home, Languages, Moon, Sun, UserCircle, X } from 'lucide-react'; import { Link, useLocation, useNavigate } from 'react-router-dom'; import { useState } from 'react'; import { useStore } from '../store';
const labels={orders:'অর্ডার',customers:'কাস্টমার',inventory:'ইনভেন্টরি',employees:'কর্মী',attendance:'হাজিরা',reports:'রিপোর্টস',settings:'সেটিংস'};
export default function Header({ onMenu }){const location=useLocation();const navigate=useNavigate();const user=useStore(s=>s.user);const theme=useStore(s=>s.theme);const setTheme=useStore(s=>s.setTheme);const logout=useStore(s=>s.logout);const [profile,setProfile]=useState(false);const [notifications,setNotifications]=useState(false);const [language,setLanguage]=useState(localStorage.getItem('factory_language')||'bn');const crumbs=location.pathname.split('/').filter(Boolean);function toggleLanguage(){const next=language==='bn'?'en':'bn';localStorage.setItem('factory_language',next);setLanguage(next)}return <><header className="top-header"><div className="header-brand"><button className="icon-btn header-menu" aria-label="মেনু খুলুন" onClick={onMenu}>☰</button><span>Two M-s Veil</span></div><div className="header-actions"><button className="icon-btn header-control" aria-label="ভাষা পরিবর্তন" onClick={toggleLanguage}><Languages size={18}/><small>{language.toUpperCase()}</small></button><button className="icon-btn header-control" aria-label="থিম পরিবর্তন" onClick={()=>setTheme(theme==='dark'?'light':'dark')}>{theme==='dark'?<Sun size={18}/>:<Moon size={18}/>}</button><div className="header-popover-wrap"><button className="icon-btn header-control notification-control" aria-label="নোটিফিকেশন" aria-expanded={notifications} onClick={()=>{setNotifications(!notifications);setProfile(false)}}><Bell size={19}/><i/></button>{notifications&&<div className="header-popover notification-popover"><div className="popover-head"><b>নোটিফিকেশন</b><button className="icon-btn" aria-label="বন্ধ করুন" onClick={()=>setNotifications(false)}><X size={15}/></button></div><div className="notification-item"><span className="notification-dot info"/><div><b>ফ্যাক্টরি ড্যাশবোর্ড প্রস্তুত</b><small>এখনই</small></div></div><div className="notification-item"><span className="notification-dot warning"/><div><b>স্টক সতর্কতা চেক করুন</b><small>আজ</small></div></div><button className="mark-read">সব পড়া হয়েছে</button></div>}</div><div className="header-popover-wrap"><button className="profile-trigger" aria-label="প্রোফাইল মেনু" aria-expanded={profile} onClick={()=>{setProfile(!profile);setNotifications(false)}}><span className="profile-avatar">{(user?.name||'U').slice(0,1).toUpperCase()}</span><span className="profile-copy"><b>{user?.name||'User'}</b><small>{user?.role||'staff'}</small></span><ChevronDown size={15}/></button>{profile&&<div className="header-popover profile-popover"><div className="profile-popover-head"><UserCircle size={27}/><div><b>{user?.name||'User'}</b><small>{user?.email||''}</small></div></div><Link to="/profile" onClick={()=>setProfile(false)}>প্রোফাইল</Link><Link to="/settings" onClick={()=>setProfile(false)}>সেটিংস</Link>{user?.role==='admin'&&<Link to="/security/2fa" onClick={()=>setProfile(false)}>2FA Setup</Link>}<button onClick={()=>{logout();navigate('/login')}}>লগ আউট</button></div>}</div></div></header><nav className="breadcrumbs" aria-label="ব্রেডক্রাম্ব"><Link to="/"><Home size={14}/> হোম</Link>{crumbs.map((crumb,index)=><span key={crumb}><b>›</b>{index===crumbs.length-1?<strong>{labels[crumb]||crumb}</strong>:<Link to={'/'+crumb}>{labels[crumb]||crumb}</Link>}</span>)}</nav></>}
