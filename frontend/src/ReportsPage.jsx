import { useEffect,useState } from 'react'; import { Download,FileText,Package,TrendingUp } from 'lucide-react'; import { get } from './api'; import { useStore } from './store'; import { Button,Empty,FormField,Loading,PageHeader } from './components/UI';
export default function ReportsPage(){const [financial,setFinancial]=useState(null);const [orders,setOrders]=useState([]);const [valuation,setValuation]=useState([]);const [range,setRange]=useState({from:'',to:''});const setToast=useStore(s=>s.setToast);async function load(){try{const q=range.from||range.to?`?from=${range.from}&to=${range.to}`:'';const [f,o,v]=await Promise.all([get(`/reports/financial${q}`),get(`/reports/profit-loss${q}`),get('/reports/valuation')]);setFinancial(f.data);setOrders(o.data);setValuation(v.data)}catch(e){setToast(e.message)}}useEffect(()=>{load()},[]);if(!financial)return <Loading/>;const cards=[['মোট আয়',financial.totalRevenue],['মোট COGS',financial.totalCOGS],['মোট লাভ',financial.totalProfit],['মোট ক্ষতি',financial.totalLoss],['ডেলিভারি হয়েছে',financial.deliveredCount],['মোট অর্ডার',financial.totalOrders]];return <><PageHeader icon={FileText} title="রিপোর্টস" subtitle="সম্পূর্ণ আর্থিক ও ইনভেন্টরি বিশ্লেষণ" action={<Button variant="outline" icon={Download} onClick={()=>window.print()}>PDF রিপোর্ট</Button>}/><div className="stats-grid report-stats">{cards.map(([label,value])=><div className="stat-card" key={label}><div><span className="stat-label">{label}</span><strong>{label.includes('হয়েছে')||label.includes('অর্ডার')?value:`৳ ${value.toFixed(2)}`}</strong></div><div className="stat-icon"><TrendingUp size={19}/></div></div>)}</div><section className="panel filter-panel"><div><h3>ফিল্টার রিপোর্ট</h3><p>তারিখ অনুযায়ী রিপোর্ট দেখুন</p></div><div className="filter-fields"><FormField label="তারিখ থেকে"><input type="date" value={range.from} onChange={e=>setRange({...range,from:e.target.value})}/></FormField><FormField label="তারিখ পর্যন্ত"><input type="date" value={range.to} onChange={e=>setRange({...range,to:e.target.value})}/></FormField><Button onClick={load}>ফিল্টার</Button></div></section><section className="panel table-panel"><div className="panel-head"><h3>লাভ/ক্ষতি বিস্তারিত</h3></div><div className="table-wrap"><table><thead><tr><th>কাস্টমার</th><th>পণ্য</th><th>বিক্রয় মূল্য</th><th>COGS (FIFO)</th><th>লাভ/ক্ষতি</th><th>অর্ডার তারিখ</th></tr></thead><tbody>{orders.map(o=><tr key={o._id}><td>{o.customer_name}</td><td>{o.product_type} (x{o.quantity})</td><td>৳ {o.total_price.toFixed(2)}</td><td>৳ {o.cogs.toFixed(2)}</td><td className={o.profit<0?'danger':'green'}>৳ {o.profit.toFixed(2)}</td><td>{new Date(o.order_date).toLocaleDateString('bn-BD')}</td></tr>)}</tbody></table>{!orders.length&&<Empty/>}</div></section><section className="panel table-panel"><div className="panel-head"><h3><Package size={17}/> ইনভেন্টরি ভ্যালুয়েশন (FIFO)</h3></div><div className="table-wrap"><table><thead><tr><th>পণ্য</th><th>ক্যাটাগরি</th><th>মোট পরিমাণ</th><th>FIFO খরচ (গড়)</th><th>মোট মূল্য</th></tr></thead><tbody>{valuation.map(i=><tr key={i.product_name}><td><b>{i.product_name}</b></td><td>{i.category}</td><td>{i.total_qty}</td><td>৳ {i.avg_cost.toFixed(2)}</td><td><b>৳ {i.total_value.toFixed(2)}</b></td></tr>)}</tbody></table></div></section></>}
