import { useState } from 'react';
import { Link, useLocation, useNavigate } from 'react-router-dom';
import { Shirt, ArrowRight, Eye, EyeOff } from 'lucide-react';
import { useStore } from '../store';
export default function Auth({ mode='login' }) { const isLogin=mode==='login'; const [form,setForm]=useState({name:'',email:'',password:''}); const [show,setShow]=useState(false); const [error,setError]=useState(''); const [busy,setBusy]=useState(false); const action=isLogin?useStore(s=>s.login):useStore(s=>s.register); const navigate=useNavigate(); const location=useLocation(); async function submit(e){e.preventDefault();setBusy(true);setError('');try{await action(form);navigate(location.state?.from?.pathname||'/')}catch(e){setError(e.message)}finally{setBusy(false)}} return <div className="auth-page"><div className="auth-visual"><div className="auth-visual-content"><div className="logo-lockup"><Shirt size={30}/><span>Two M-s Veil</span></div><div><p className="eyebrow">PREMIUM FACTORY OPERATIONS</p><h1>কারখানার প্রতিটি<br/><em>সুতোয় নিয়ন্ত্রণ।</em></h1><p className="visual-copy">অর্ডার, ইনভেন্টরি, কর্মী এবং হিসাব এক জায়গায়।</p></div><div className="visual-line"/></div></div><div className="auth-card"><div className="auth-top"><span className="mobile-logo"><Shirt size={23}/> Two M-s Veil</span><span className="auth-switch">{isLogin?'নতুন ব্যবহারকারী?':'অ্যাকাউন্ট আছে?'} <Link to={isLogin?'/register':'/login'}>{isLogin?'রেজিস্টার করুন':'লগইন করুন'}</Link></span></div><div className="auth-form"><p className="eyebrow">{isLogin?'WELCOME BACK':'GET STARTED'}</p><h2>{isLogin?'আবার দেখা হলো':'অ্যাকাউন্ট তৈরি করুন'}</h2><p className="muted">{isLogin?'আপনার কারখানার ড্যাশবোর্ডে প্রবেশ করুন।':'ফ্যাক্টরি ম্যানেজমেন্ট শুরু করতে তথ্য দিন।'}</p>{error&&<div className="form-error">{error}</div>}<form onSubmit={submit}>{!isLogin&&<label className="form-field"><span>নাম</span><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="আপনার পূর্ণ নাম"/></label>}<label className="form-field"><span>ইমেইল</span><input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="name@company.com"/></label><label className="form-field"><span>পাসওয়ার্ড</span><div className="password-input"><input required minLength={6} type={show?'text':'password'} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="কমপক্ষে ৬ অক্ষর"/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label><button className="btn btn-primary auth-submit" disabled={busy}>{busy?'অপেক্ষা করুন...':isLogin?'লগইন করুন':'অ্যাকাউন্ট খুলুন'}<ArrowRight size={17}/></button></form></div><p className="auth-footer">সুরক্ষিত ফ্যাক্টরি অপারেশন প্ল্যাটফর্ম · ২০২৬</p></div></div> }
