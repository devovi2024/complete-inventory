import { useEffect,useState } from 'react'; import { BarChart3,CheckCircle2, CircleDollarSign,Package,ShoppingCart,TrendingDown,FileText,Boxes,Plus } from 'lucide-react'; import { get } from '../api'; import { useStore } from '../store'; import { Button,Loading,PageHeader } from '../components/UI'; import '../dashboard-extra.css';
export default function DashboardPage(){const [financial,setFinancial]=useState(null);const [inventory,setInventory]=useState([]);const [orders,setOrders]=useState([]);const setToast=useStore(s=>s.setToast);useEffect(()=>{Promise.all([get('/reports/financial'),get('/orders'),get('/inventory')]).then(([f,o,i])=>{setFinancial(f.data);setOrders(o.data);setInventory(i.data)}).catch(e=>setToast(e.message))},[setToast]);if(!financial)return <Loading/>;const stats=[['মোট অর্ডার',financial.totalOrders,ShoppingCart],['ডেলিভারি হয়েছে',financial.deliveredCount,CheckCircle2],['মোট লাভ',`৳ ${financial.totalProfit.toFixed(2)}`,TrendingDown],['মোট ক্ষতি',`৳ ${financial.totalLoss.toFixed(2)}`,TrendingDown],['ইনভেন্টরি মূল্য',`৳ ${inventory.reduce((s,i)=>s+i.total_qty*i.avg_cost,0).toFixed(2)}`,Package],['মোট বাকি',`৳ ${orders.reduce((s,o)=>s+o.due_amount,0).toFixed(2)}`,CircleDollarSign]];return <><PageHeader icon={BarChart3} title="ড্যাশবোর্ড" subtitle="আপনার ফ্যাক্টরির সার্বিক অবস্থা — রিয়েল-টাইম আপডেট" action={<Button icon={Plus}>নতুন অর্ডার</Button>}/><div className="stats-grid">{stats.map(([label,value,Icon])=><div className="stat-card" key={label}><div><span className="stat-label">{label}</span><strong>{value}</strong></div><div className="stat-icon"><Icon size={21}/></div></div>)}</div><div className="dashboard-grid"><section className="panel"><div className="panel-head"><div><h3>অর্ডার স্ট্যাটাস</h3><p>বর্তমান অর্ডারগুলোর অবস্থান</p></div></div><div className="dashboard-status-list">{['Pending','Processing','Ready','Delivered'].map(status=><div key={status}><span className={`status status-${status.toLowerCase()}`}>{status}</span><b>{orders.filter(o=>o.status===status).length}</b></div>)}</div></section><section className="panel"><div className="panel-head"><div><h3>মাসিক অর্ডার ট্রেন্ড</h3><p>সাম্প্রতিক অর্ডারের সারাংশ</p></div></div><div className="trend-bars">{orders.slice(0,10).map((order,index)=><i key={order._id} style={{height:`${30+(index%5)*14}px`}} title={order.product_type}/>)}</div></section></div><section className="panel quick-actions"><div className="panel-head"><div><h3>দ্রুত অ্যাকশন</h3><p>প্রতিদিনের কাজ দ্রুত শুরু করুন</p></div></div><div><Button icon={Plus}>নতুন অর্ডার</Button><Button icon={Plus}>নতুন স্টক</Button><Button variant="outline" icon={FileText}>রিপোর্ট দেখুন</Button><Button variant="outline" icon={Boxes}>ইনভেন্টরি</Button></div></section></>}
