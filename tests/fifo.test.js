import test from 'node:test'; import assert from 'node:assert/strict'; import { addFIFOLayer, calculateAvgCost, consumeFIFO } from '../utils/fifo.js';
test('FIFO consumes oldest lots and recalculates valuation', () => { const item = { fifo_layers: [] }; addFIFOLayer(item, { quantity: 5, unitCost: 10, purchaseDate: '2024-01-01' }); addFIFOLayer(item, { quantity: 5, unitCost: 14, purchaseDate: '2024-02-01' }); const result = consumeFIFO(item, 7); assert.equal(result.cogs, 78); assert.equal(item.total_qty, 3); assert.equal(calculateAvgCost(item), 14); });
test('FIFO rejects insufficient stock without mutating', () => { const item = { fifo_layers: [] }; addFIFOLayer(item, { quantity: 2, unitCost: 10 }); assert.throws(() => consumeFIFO(item, 3), /Insufficient inventory/); assert.equal(item.total_qty, 2); });
